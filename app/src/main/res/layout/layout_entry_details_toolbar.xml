<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.AppBarLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:elevation="@dimen/toolbar_elevation">

    <android.support.design.widget.CollapsingToolbarLayout
        android:id="@+id/entry_details_activity_collapse_toolbar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        app:background="?attr/colorPrimary"
        app:collapsedTitleTextAppearance="@style/ToolbarTitleTextAppearance"
        app:contentScrim="?attr/colorPrimary"
        app:expandedTitleMargin="16dp"
        app:expandedTitleMarginEnd="16dp"
        app:expandedTitleTextAppearance="@style/ToolbarLargeTextAppearance"
        app:layout_scrollFlags="scroll|exitUntilCollapsed|snap"
        app:titleEnabled="true">

        <ImageView
            android:id="@+id/entry_details_activity_toolbar_photo_image_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/entry_details_toolbar_height"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_no_image_placeholder"
            app:layout_collapseMode="parallax" />

        <android.support.v7.widget.Toolbar
            android:id="@+id/entry_details_activity_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:layout_collapseMode="pin"
            app:navigationIcon="@drawable/ic_back_white" />

    </android.support.design.widget.CollapsingToolbarLayout>

</android.support.design.widget.AppBarLayout>